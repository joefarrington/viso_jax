# @package _global_

defaults:
  - override /rollout_wrapper: rajendran_perishable_platelet
  - override /scenario_settings: rajendran_perishable_platelet_base_settings
  - override /policy: sS_policy

exp_name: rajendran_perishable_platelet/sS_policy

param_search:
  sampler:
    _target_: optuna.samplers.NSGAIISampler
    population_size: 50
  max_iterations: 200
  early_stopping_rounds: 20